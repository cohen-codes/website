---
description: A component that renders the work history of the user.
labels: ["work experience", "history"]
---

import { ThemeProvider } from "@mui/material";
import { History } from "./history.js";

A component that does renders the work history of a person

### Component usage

Use with `ThemeProvider` to enforce styling

```js
<History />
```

### Render sample history

```js live
<ThemeProvider>
  <History />
</ThemeProvider>
```
