---
description: A component that creates a Page for the project list
labels: ["page", "project list page"]
---

import { MemoryRouter } from "react-router-dom";
import { ProjectsPage } from "./projects-page.js";

A component that renders the project page.

### Component usage

Use with a `ThemeProvider` to apply the styles required.
Use with a React Router DOM Implementation to use routing.

```js
<MemoryRouter>
  <Projectsage />
</MemoryRouter>
```

### Render a project page.

```js live
<MemoryRouter>
  <Projectsage />
</MemoryRouter>
```
