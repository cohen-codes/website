---
description: A component that creates a Page for `About`
labels: ["page", "about page"]
---

import { Main } from "@nitsan770/portfolio.layouts.main";
import { MemoryRouter } from "react-router-dom";
import { AboutPage } from "./about-page.js";

A component that renders an About Me Page

### Component usage

Use with a `ThemeProvider` for styling.
Use with a React Router to enable routing.

```js
<MemoryRouter>
  <AboutPage />
</MemoryRouter>
```

### Rendering the About Us page.

```js live
<MemoryRouter>
  <Main>
    <AboutPage />
  </Main>
</MemoryRouter>
```
